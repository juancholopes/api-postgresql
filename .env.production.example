# Variables de entorno para Producción
# Copiar a .env.production y cambiar todos los valores

# Base de datos
DB_NAME=authdb
DB_USER=postgres
DB_PASSWORD=CAMBIAR_POR_CONTRASEÑA_SEGURA

# JWT - Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIAR_POR_SECRET_SEGURO_DE_64_CARACTERES

# Nginx
NGINX_PORT=80
NGINX_SSL_PORT=443
